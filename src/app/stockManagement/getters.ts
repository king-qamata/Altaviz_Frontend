import { IStockManagementState } from './state';

export interface IStockManagementGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<IStockManagementState, IState>, param: any): Promise<any>;
   */
}

export const StockManagementGetters: IStockManagementGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: IStockManagementState) {
   *   return state.myAttribute;
   * }
   */
};
