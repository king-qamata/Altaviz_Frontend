import { IStockManagementState } from './state';

export interface IStockManagementMutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<IStockManagementState, IState>, param: any): void;
   */
}

export const StockManagementMutations: IStockManagementMutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
