import { IIncidentManagementState } from './state';

export interface IIncidentManagementGetters {
  /**
   * put your getter names, parameters and return values here, for example:
   * myAction(context: ActionContext<IIncidentManagementState, IState>, param: any): Promise<any>;
   */
}

export const IncidentManagementGetters: IIncidentManagementGetters = {
  /**
   * here comes the implementation of your getters, for example:
   * myGetter(state: IIncidentManagementState) {
   *   return state.myAttribute;
   * }
   */
};
