import { IncidentManagementMutations } from './mutations';
import { IncidentManagementDefaultState, IIncidentManagementState } from './state';

describe('IncidentManagementMutations', () => {
  let testState: IIncidentManagementState;

  beforeEach(() => {
    testState = IncidentManagementDefaultState();
  });

  test('Please write the tests for the mutations here', () => {
    // here is an example: https://github.com/vuesion/vuesion/blob/master/src/app/counter/mutations.spec.ts
    expect(true).toBeFalsy();
  });
});
