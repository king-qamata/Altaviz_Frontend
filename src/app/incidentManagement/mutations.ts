import { IIncidentManagementState } from './state';

export interface IIncidentManagementMutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<IIncidentManagementState, IState>, param: any): void;
   */
}

export const IncidentManagementMutations: IIncidentManagementMutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
