import { IAtmEngineerManagementState } from './state';

export interface IAtmEngineerManagementMutations {
  /**
   * put your mutations names, parameters and return values here, for example:
   * MY_MUTATION(context: ActionContext<IAtmEngineerManagementState, IState>, param: any): void;
   */
}

export const AtmEngineerManagementMutations: IAtmEngineerManagementMutations = {
  /**
   * here comes the implementation of your mutations, for example:
   * MY_MUTATION: (state, param) {
   *   return state.myAttribute = param;
   * }
   */
};
